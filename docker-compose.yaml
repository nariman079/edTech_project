services:
  backend:
    build: .
    command: poetry run python backend/manage.py migrate && python backend/manage.py runserver 
    volumes:
      - ./:/app
    restart: unless-stopped
    network_mode: 'host'

  load_balancer:
    image: nginx
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./frontend/dist:/app/frontend
      - /etc/letsencrypt:/etc/letsencrypt/
    depends_on:
      - backend
    network_mode: 'host'
